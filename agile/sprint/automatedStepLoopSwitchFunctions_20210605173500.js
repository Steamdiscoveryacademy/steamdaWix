export function ppMemberPrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('ppMemberPrepJSON','ppMemberPrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('ppMemberPrepJSON');
    }
    
    export function ppMemberExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('ppMemberExecuteUpsert set on ' + timeDateString);
    }
    
    export function stMemberPrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('stMemberPrepJSON','stMemberPrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('stMemberPrepJSON');
    }
    
    export function stMemberExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('stMemberExecuteUpsert set on ' + timeDateString);
    }
    
    export function ppContactPrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('ppContactPrepJSON','ppContactPrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('ppContactPrepJSON');
    }
    
    export function ppDatabasePrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('ppDatabasePrepJSON','ppDatabasePrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('ppDatabasePrepJSON');
    }
    
    export function stContactPrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('stContactPrepJSON','stContactPrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('stContactPrepJSON');
    }
    
    export function stDatabasePrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('stDatabasePrepJSON','stDatabasePrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('stDatabasePrepJSON');
    }
    
    export function spContactPrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('spContactPrepJSON','spContactPrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('spContactPrepJSON');
    }
    
    export function spDatabasePrepJSON(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        memory.setItem('spDatabasePrepJSON','spDatabasePrepJSON' + ' set on ' + timeDateString);
        console.log(memory.getItem('spDatabasePrepJSON');
    }
    
    export function ppContactExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('ppContactExecuteUpsert set on ' + timeDateString);
    }
    
    export function ppDatabaseExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('ppDatabaseExecuteUpsert set on ' + timeDateString);
    }
    
    export function stContactExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('stContactExecuteUpsert set on ' + timeDateString);
    }
    
    export function stDatabaseExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('stDatabaseExecuteUpsert set on ' + timeDateString);
    }
    
    export function spContactExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('spContactExecuteUpsert set on ' + timeDateString);
    }
    
    export function spDatabaseExecuteUpsert(){
        let now = new Date();
        let timeDateString = ' [ on ' + now.toLocaleDateString() + ']';
        timeDateString = now.toLocaleTimeString('en-US') + timeDateString;
        console.log('spDatabaseExecuteUpsert set on ' + timeDateString);
    }